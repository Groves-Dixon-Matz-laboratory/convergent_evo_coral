#Ran CC, MF, and BP for each of the four input files.

#ran with:
	goAnnotations="singleCopyAnnotations_GO_gomwu.tsv" #new better version made from goa_uniprot_all.gaf
	largest = 0.1
	smallest = 200
	clusterCutHeight = 0.25
	Alternative = 'g'

#when there were significant results, the tree was saved.
#The top 20 GO terms were saved regardless of whether any were significant.

